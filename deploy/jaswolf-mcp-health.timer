# Runs the health probe every 2 minutes (and 2 min after boot).
[Unit]
Description=Periodic JasWolf MCP health probe

[Timer]
OnBootSec=2min
OnUnitActiveSec=2min
AccuracySec=15s

[Install]
WantedBy=timers.target
